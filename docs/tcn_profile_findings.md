# TCN 离线验证：2026-09-13

已执行 profile_tcn.py，使用实战报告指向的 step_000097000_1789265318471816453.pt，RTX3050 Laptop GPU，PyTorch 2.13.0+cu132。未启动游戏，未编译前端。使用随机预编码特征常驻 GPU，不包含状态构建、PCIe 输入搬运、发键；未控制外部 GPU 负载/温度，顺序测试有波动，不作为稳定部署 SLA。

每种长度10次预热、30次同步计时，profiler 单独执行不混入延迟样本。测试1/2/3/4/8/18帧，FP32和AMP两套。与同精度参考增量路径连续3段的最后输出比较均通过，本次 max_abs_error=0；这不是全部真实输入或完整模型动作等价性证明。

AMP 单帧普通路径 P50/P95=13.93/18.12ms，Graph=1.57/1.73ms。AMP 两帧普通路径=15.91/19.91ms，当前路由=19.62/39.98ms；两帧路由实际上是普通批量前向，不是批量 Graph，不能据此评价批量 Graph 性能。FP32单帧普通=25.64/31.24ms，Graph=6.65/6.85ms。

AMP 两帧 profiler 中 cudnn_convolution 15次 CUDA累计约0.854ms，其中布局转换kernel累计约0.675ms（包含在卷积内，不能重复相加）；小操作、复制及主机提交也有明显开销。单帧AMP Graph的profile中也存在布局转换和类型转换kernel。说明不能把15~20ms直接解释成卷积数学运算耗时；Graph单帧实验支持提交路径优化，但多帧Graph尚未实现验证。

结论：优先独立实现并验证2/3/4帧固定批量Graph，正确衔接共享缓存；其次评估减少布局/类型转换。先通过相同输入对照，再接实战，不承诺60Hz。当前实战多阶段计时已停用，首页移除详细分组表；整体延迟与轻量循环原始记录保留。

报告：outputs/tcn_profile_fp32、outputs/tcn_profile_amp；每个目录包含summary.json、每种路径/长度的profiler表和Chrome trace。脚本参数 --output 必须指定不存在的目录，避免覆盖报告。
