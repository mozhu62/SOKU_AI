# 批量 CUDA Graph 离线验收

2026-09-13，RTX3050 Laptop，使用 step_000097000_1789265318471816453.pt。候选实现 batch_graph_candidate.py 仅由 profile_tcn.py 的 --batch-graph 参数启用，未改实战默认路由。

为2/3/4/8/18帧分别捕获完整批量前向；单帧复用单帧Graph，各图共用逐层缓存，各自独立工作区。未捕获长度走普通批量路径。首次满窗初始化覆盖捕获预热状态。

FP16 autocast，每项10次预热、50次计时，profiler另行采集：

| 新增帧 | 普通路径P50 ms | 批量Graph P50/P95 ms |
|---|---:|---:|
|2|9.92|1.54 / 1.95|
|3|9.60|1.45 / 1.69|
|4|8.90|1.30 / 1.58|
|8|8.56|1.55 / 1.76|
|18|8.75|1.92 / 2.09|

FP32及FP16均完成验证：固定长度连续输出以及256/1/2/3/4/8/18/5/3/1/64/2长度切换、两次重置，与同精度参考输出比较通过；本轮最大绝对误差0。5/64帧用于检查未捕获长度回退后再次进入Graph的缓存连续性。不是完整游戏输入或最终动作等价性的证明。

输入是GPU常驻随机预编码状态；不含CPU编码、上传、最终特征回传及游戏竞争。外部负载和频率未受控，结果不能作为60Hz保证，FP32/FP16分次测量不宜直接排名。计时每次同步，代表隔离请求延迟，非异步吞吐。

报告 outputs/tcn_batch_graph_amp 和 outputs/tcn_batch_graph_fp32 含summary、算子表、Chrome trace。已运行离线验证，未启动游戏、未构建前端、未接入实战。下一步可选择接入该候选，再做同配置端到端对照。
